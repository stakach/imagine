name: imagine
version: 0.1.0

dependencies:
  # Video stream parsing
  ffmpeg:
    github: spider-gazelle/ffmpeg
    version: ~> 0.4

  # Load and run pre-built neural nets for image recognition
  tensorflow_lite:
    github: spider-gazelle/tensorflow_lite
    version: ~> 0.9

development_dependencies:
  ameba:
    github: veelenga/ameba
